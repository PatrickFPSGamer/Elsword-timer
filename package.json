{
  "name": "elsword-combo-timer",
  "version": "1.0.0",
  "description": "Combo Timer for Elsword",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "start-admin": "powershell Start-Process electron -ArgumentList . -Verb RunAs"
  },
  "dependencies": {
    "node-global-key-listener": "^0.1.1"
  },
  "devDependencies": {
    "electron": "^28.1.0"
  }
}
